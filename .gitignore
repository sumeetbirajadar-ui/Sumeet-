node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Capacitor-generated native Android projects (large, fully reproducible via
# `npx cap add android && npx capacitor-assets generate --android` in each
# mobile/<app>/ folder) — kept out of git, shipped to the user as a zip instead.
mobile/*/android/

# Downloaded PDFs from the mathongo-pdf-downloader tool
tools/mathongo-pdf-downloader/downloads/
